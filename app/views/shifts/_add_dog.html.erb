<%= form_for shift, method: :put do |f| %>
    <%= f.select :dogs, options_for_select(@unallocated_dogs.map { |d| [d.name, d.id] }) %>
    <%= f.submit 'Add', class: 'py-2 px-4 bg-blue-200 rounded' %>
<% end %>